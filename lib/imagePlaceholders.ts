/**
 * Use local assets for fallbacks so Next/Image never depends on remote URLs that may 404.
 * Replace with a dedicated marketing asset later if you add one to /public.
 */
export const LOCAL_IMAGE_FALLBACK = "/Artisan-logo.jpg";
